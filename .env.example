# Production Environment Configuration Template
# Copy this to .env in production and update values

# ======================
# FRONTEND CONFIGURATION
# ======================
# Update with your actual server IP or domain
REACT_APP_BACKEND_URL=http://YOUR_SERVER_IP:8001

# ======================
# BACKEND CONFIGURATION
# ======================
# MongoDB connection (local Docker or external)
MONGO_URL=mongodb://mongodb:27017
DB_NAME=network_scanner

# CORS allowed origins (comma-separated)
# In production, set to your actual frontend URL
CORS_ORIGINS=http://YOUR_SERVER_IP:3000,http://YOUR_DOMAIN.com

# ======================
# SECURITY (Optional)
# ======================
# MongoDB Authentication (recommended for production)
# MONGO_USERNAME=admin
# MONGO_PASSWORD=your_secure_password_here

# ======================
# DEPLOYMENT NOTES
# ======================
# 1. For Docker deployment: Only change REACT_APP_BACKEND_URL to your server IP
# 2. For Emergent deployment: Environment variables are auto-configured
# 3. For production: Enable MongoDB authentication and use HTTPS
# 4. Network scanning requires NET_ADMIN capability in Docker
